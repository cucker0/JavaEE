<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.dao.EmployeeDynamicSqlMapper">
    <!-- 会涉及使用OGNL表达式 -->

    <!-- List<Employee> getEmployeesByConditionIf(Employee employee); -->
    <select id="getEmployeesByConditionIf" resultType="com.java.bean.Employee">
        <!-- 这种写法有缺点，WHERE后的第一个查询条件为空是，将报语法错误，因为会导致SQl语句WHERE后多了一个AND -->
        SELECT id, last_name lastName, gender, email FROM t_employee_x
        WHERE
        <if test="id != null">
            id = #{id}
        </if>
        <if test="lastName != null">
            AND last_name = #{lastName}
        </if>
        <!-- 会自动传入参数的gender属性转数字，原来字符串 -->
        <if test="gender == 0 or gender == 1">
            AND gender = #{gender}
        </if>
        <if test="email != null and email.trim() != ''">
            AND email = #{email}
        </if>
    </select>

    <!-- List<Employee> getEmployeesByConditionIf2(Employee employee); -->
    <select id="getEmployeesByConditionIf2" resultType="com.java.bean.Employee">
        SELECT id, last_name lastName, gender, email FROM t_employee_x
        <where>
            <if test="id != null">
                id = #{id}
            </if>
            <if test="lastName != null">
                AND last_name = #{lastName}
            </if>
            <if test="gender == 0 or gender == 1">
                AND gender = #{gender}
            </if>
            <if test="email != null and email.trim() != ''">
                AND email = #{email}
            </if>
        </where>
    </select>
</mapper>